<%= form_for(release_group) do |f| %>
  <% if release_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(release_group.errors.count, "error") %> prohibited this release_group from being saved:</h2>

      <ul>
      <% release_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :record_label %>
    <%= f.text_field :record_label %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field :image_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
